<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Main Menu -->
    <menuitem id="estate_main_menu"
        name="Real Estate"
        sequence="10" />

    <!-- Submenu -->
    <menuitem id="advertisement_sub_menu"
        name="Advertisements"
        parent="estate_main_menu"
        sequence="10" />

    <menuitem
        id="setting_sub_menu"
        name="Settings"
        parent="estate_main_menu"
        sequence="20" />

    <!-- Menu items -->
    <menuitem
        id="model_estate_property_menu"
        name="Properties"
        parent="advertisement_sub_menu"
        action="estate_property_action"
        sequence="10" />

    <menuitem
        id="estate_property_type_menu"
        name="Property Types"
        action="estate_property_type_action"
        parent="setting_sub_menu"
        sequence="10" />

    <menuitem
        id="estate_property_tag_menu"
        name="Property Tags"
        action="estate_property_tag_action"
        parent="setting_sub_menu"
        sequence="20" />
</odoo>
